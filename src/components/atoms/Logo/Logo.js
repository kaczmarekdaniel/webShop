import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  width: 200px;
  height: 50px;
  display: flex;
  background-color: black;
`;

const Logo = ({ text }) => <Wrapper></Wrapper>;

export default Logo;
